const config = require("./config");
const catalog = require("./catalog");
const database = require("./database");

console.log("NZSTORE BOT STARTED");
console.log("Total produk:", Object.keys(catalog).length);
